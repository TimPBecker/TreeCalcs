% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tree_interface.R
\name{permute_tree}
\alias{permute_tree}
\title{Permute tree subtrees across all levels}
\usage{
permute_tree(tree, as_data_tree = NULL)
}
\arguments{
\item{tree}{A `data.tree` Node or nested tree list.}

\item{as_data_tree}{Logical; whether to return results as `data.tree` Node objects.
Defaults to TRUE if input was a data.tree Node, or if data.tree is installed.}
}
\value{
A list of permuted trees (`data.tree` nodes or nested lists).
}
\description{
Generates all distinct tree permutations by sorting and permuting child subtrees
at every internal node level using C++20.
}
\examples{
\dontrun{
root <- tree_node("root", list(
  tree_node("group1", list(tree_leaf("a", 8), tree_leaf("b", 2))),
  tree_node("group2", list(tree_leaf("c", 5), tree_leaf("d", 1)))
))
permuted <- permute_tree(root)
length(permuted) # 8 distinct trees
}
}
